app.config(function($stateProvider, $urlRouterProvider) {
    $stateProvider
    .state('menu', {
            url: "/dashboard/menu",
            views: {
                "appView": {
                    templateUrl: "/views/cpanel/menu/menu.html"
                }
            }
        })
	$urlRouterProvider.otherwise('/dashboard');
});